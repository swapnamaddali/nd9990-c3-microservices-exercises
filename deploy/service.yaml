apiVersion: v1
kind: Service
metadata:
  labels:
    app: udafrontend
  name: udafrontend-svc
  namespace: default
  selfLink: /api/v1/namespaces/default/services/udafrontend-svc
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: udafrontend
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
